import { Typography, Box } from "@mui/material";

export default function Dashboard() {
  return (
    <Box>
      <Typography variant="h5">Welcome to SupportDesk Dashboard</Typography>
      <Typography variant="body1">Use the sidebar to manage tickets, agents, and settings.</Typography>
    </Box>
  );
}